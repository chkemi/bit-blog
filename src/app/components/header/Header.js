import React from 'react';
import Nav from './Nav';

const Header = (props) => {
    return (
        <header>
            <h1>BIT BLOG</h1>
            <Nav />
        </header>
    );
}

export default Header;